%h2 Session Data

%table
  %tr
    %th Key
    %th Value
  - unless @facebook_auth_data.nil?
    - @facebook_auth_data.keys.each do |key|
      %tr
        %td= key
        %td= @facebook_auth_data[key].inspect
  - else
    %tr
      %td{:colspan => 2, :align => 'center'} No Session Data
